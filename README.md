# WildfireServer

## Setup

Install erlang, elixir, and node:

```bash
  asdf install
```

Get dependencies:

```bash
  mix deps.get
```

Start the server:

```bash
  iex -S mix
```

Open a browser and navigate to http://localhost:4000
